{
  // The configuration of the durability bar
  "DurabilityBar": {
    // Whether the status bar should be enabled
    // Default: True
    "Enabled": true,

    // Decides whether providers are allowed to override the configured color.
    // Disabling this will make it always use and disable any dynamic color decissions like with the durability bar.
    "AllowColorOverride": true,

    // The color that should be used for this status bar.
    "Color": {
      "R": 0.482,
      "G": 0.521,
      "B": 0.211,
      "A": 0.5
    },

    // Wether the bar should show up even if it is at 100% filled.
    "ShowEvenIfBarFull": false,

    // The type of bar that should be used
    // Valid Values: Horizontal (0), Vertical (1), FloodFill (2)
    "HudBarType": "Horizontal",

    // Extra styling rules, some of these might not be applicable depending on the
    // Valid Values (Combination):None (0), Reverse (1), NoBackground (2), AlternativeAlignment (4)
    "HudBarStyle": "None"
  },

  // The configuration of the condition bar shown on wearable items
  "ClothingConditionBar": {
    // Whether the status bar should be enabled
    // Default: True
    "Enabled": true,

    // Decides whether providers are allowed to override the configured color.
    // Disabling this will make it always use and disable any dynamic color decissions like with the durability bar.
    "AllowColorOverride": true,

    // The color that should be used for this status bar.
    "Color": {
      "R": 1.0,
      "G": 0.5,
      "B": 0.0,
      "A": 0.7
    },

    // Wether the bar should show up even if it is at 100% filled.
    "ShowEvenIfBarFull": false,

    // The type of bar that should be used
    // Valid Values: Horizontal (0), Vertical (1), FloodFill (2)
    "HudBarType": "Vertical",

    // Extra styling rules, some of these might not be applicable depending on the
    // Valid Values (Combination):None (0), Reverse (1), NoBackground (2), AlternativeAlignment (4)
    "HudBarStyle": "None"
  },

  // The configuration of the food portion bar shown on meals
  "FoodPortionBar": {
    // Whether the status bar should be enabled
    // Default: True
    "Enabled": true,

    // Decides whether providers are allowed to override the configured color.
    // Disabling this will make it always use and disable any dynamic color decissions like with the durability bar.
    "AllowColorOverride": false,

    // The color that should be used for this status bar.
    "Color": {
      "R": 0.482,
      "G": 0.521,
      "B": 0.211,
      "A": 0.5
    },

    // Wether the bar should show up even if it is at 100% filled.
    "ShowEvenIfBarFull": false,

    // The type of bar that should be used
    // Valid Values: Horizontal (0), Vertical (1), FloodFill (2)
    "HudBarType": "Horizontal",

    // Extra styling rules, some of these might not be applicable depending on the
    // Valid Values (Combination):None (0), Reverse (1), NoBackground (2), AlternativeAlignment (4)
    "HudBarStyle": "None"
  },

  // The configuration of the liquid bar shown on liquid containers
  "LiquidBar": {
    // Whether the status bar should be enabled
    // Default: True
    "Enabled": true,

    // Decides whether providers are allowed to override the configured color.
    // Disabling this will make it always use and disable any dynamic color decissions like with the durability bar.
    "AllowColorOverride": false,

    // The color that should be used for this status bar.
    "Color": {
      "R": 0.0,
      "G": 0.4,
      "B": 0.5,
      "A": 0.5
    },

    // Wether the bar should show up even if it is at 100% filled.
    "ShowEvenIfBarFull": true,

    // The type of bar that should be used
    // Valid Values: Horizontal (0), Vertical (1), FloodFill (2)
    "HudBarType": "Vertical",

    // Extra styling rules, some of these might not be applicable depending on the
    // Valid Values (Combination):None (0), Reverse (1), NoBackground (2), AlternativeAlignment (4)
    "HudBarStyle": "None"
  },

  // The configuration of the filled bar on bags
  "BagBar": {
    // Whether the status bar should be enabled
    // Default: True
    "Enabled": true,

    // Decides whether providers are allowed to override the configured color.
    // Disabling this will make it always use and disable any dynamic color decissions like with the durability bar.
    "AllowColorOverride": false,

    // The color that should be used for this status bar.
    "Color": {
      "R": 1.0,
      "G": 0.5,
      "B": 0.0,
      "A": 0.5
    },

    // Wether the bar should show up even if it is at 100% filled.
    "ShowEvenIfBarFull": true,

    // The type of bar that should be used
    // Valid Values: Horizontal (0), Vertical (1), FloodFill (2)
    "HudBarType": "FloodFill",

    // Extra styling rules, some of these might not be applicable depending on the
    // Valid Values (Combination):None (0), Reverse (1), NoBackground (2), AlternativeAlignment (4)
    "HudBarStyle": "None"
  },

  // Configuration regarding compatibility with other mods
  "Compatibility": {
    // The configuration of the filled bar on bags
    "ToolSmithSharpnessBar": {
      // Whether the status bar should be enabled
      // Default: True
      "Enabled": true,

      // Decides whether providers are allowed to override the configured color.
      // Disabling this will make it always use and disable any dynamic color decissions like with the durability bar.
      "AllowColorOverride": true,

      // The color that should be used for this status bar.
      "Color": {
        "R": 0.0,
        "G": 0.0,
        "B": 0.5,
        "A": 0.5
      },

      // Wether the bar should show up even if it is at 100% filled.
      "ShowEvenIfBarFull": false,

      // The type of bar that should be used
      // Valid Values: Horizontal (0), Vertical (1), FloodFill (2)
      "HudBarType": "Horizontal",

      // Extra styling rules, some of these might not be applicable depending on the
      // Valid Values (Combination):None (0), Reverse (1), NoBackground (2), AlternativeAlignment (4)
      "HudBarStyle": "None"
    }
  },
  "ExternalyRegistered": {}
}